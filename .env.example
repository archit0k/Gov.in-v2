# Gov.in — AI gateway
# Optional. Without a key the deterministic navigation engine handles everything
# and the product still works end to end; the model only adds ambiguous-intent
# routing and contextual answers inside journeys.
OPENAI_API_KEY=sk-...
# Optional override. Default: gpt-4.1-mini
# OPENAI_MODEL=gpt-4.1-mini
